export interface message{
    massage:string,
    issueId:number,
    senderId:number,
    receiverId:number,
}
export interface returnMessage{
    massage:string,
    issueId:number,
    receiverId:number,
}